# Требования для улучшенной версии JoyDance с поддержкой ESP32-C3

# Основные зависимости (оригинальные)
aiohttp>=3.8.0
hidapi

# Новые зависимости для BLE поддержки
bleak>=0.21.0  # Для работы с Bluetooth Low Energy
asyncio-mqtt>=0.11.0  # Опциональная поддержка MQTT (для альтернативной связи)

# Для улучшенного логирования и мониторинга
structlog>=23.1.0  # Структурированное логирование
psutil>=5.9.0  # Мониторинг системных ресурсов

# Для более надежной работы с конфигурацией
pydantic>=2.0.0  # Валидация настроек
toml>=0.10.2  # Поддержка TOML конфигурации

# Для разработки и тестирования (опционально)
pytest>=7.0.0
pytest-asyncio>=0.21.0
mock>=4.0.3

# Дополнительные утилиты
colorama>=0.4.6  # Цветной вывод в консоли
click>=8.1.0  # Улучшенный CLI интерфейс (опционально)
